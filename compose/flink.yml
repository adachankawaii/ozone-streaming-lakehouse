services:
  flink-jobmanager:
    build:
      context: ./flink
      dockerfile: Dockerfile
    image: ozone-lakehouse-flink:2.1.3
    hostname: flink-jobmanager
    command: jobmanager
    depends_on:
      kafka:
        condition: service_healthy
      lakekeeper:
        condition: service_healthy
      s3g:
        condition: service_healthy
    ports:
      - "8081:8081"
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        jobmanager.bind-host: 0.0.0.0
        rest.address: 0.0.0.0
        rest.bind-address: 0.0.0.0

        parallelism.default: 2

        execution.checkpointing.interval: 10s
        execution.checkpointing.mode: EXACTLY_ONCE
        execution.checkpointing.storage: filesystem
        execution.checkpointing.dir: s3a://flink-state/checkpoints
        execution.checkpointing.externalized-checkpoint-retention: RETAIN_ON_CANCELLATION
        execution.checkpointing.num-retained: 3

        fs.s3a.endpoint: http://s3.ozone:9878
        fs.s3a.path.style.access: true
        fs.s3a.connection.ssl.enabled: false
        fs.s3a.access.key: lakehouse
        fs.s3a.secret.key: lakehouse-demo-secret
    volumes:
      - ./flink/config/init.sql:/opt/flink/conf/init.sql:ro
      - ./flink/config/smoke.sql:/opt/flink/conf/smoke.sql:ro
      - ./flink/jobs:/opt/flink/jobs:ro
    networks:
      - lakehouse_net
    restart: unless-stopped

  flink-taskmanager:
    image: ozone-lakehouse-flink:2.1.3
    command: taskmanager
    depends_on:
      flink-jobmanager:
        condition: service_started
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.bind-host: 0.0.0.0
        taskmanager.numberOfTaskSlots: 4

        parallelism.default: 2

        execution.checkpointing.interval: 10s
        execution.checkpointing.mode: EXACTLY_ONCE
        execution.checkpointing.storage: filesystem
        execution.checkpointing.dir: s3a://flink-state/checkpoints
        execution.checkpointing.externalized-checkpoint-retention: RETAIN_ON_CANCELLATION
        execution.checkpointing.num-retained: 3

        fs.s3a.endpoint: http://s3.ozone:9878
        fs.s3a.path.style.access: true
        fs.s3a.connection.ssl.enabled: false
        fs.s3a.access.key: lakehouse
        fs.s3a.secret.key: lakehouse-demo-secret
    networks:
      - lakehouse_net
    restart: unless-stopped
